<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Video Trailer</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/CSS/style.css" />
  </head>
  <div class="content">
    <body>
      <h1 class="movieDetail" id="mmm">Watch the Trailer</h1>
      <h4 class="movieDetail desc"><i><%= overview %></i></h4>

      <br />
      <% const videoId = trailer.split('v=')[1].split('&')[0]; embedUrl =
      `https://www.youtube.com/embed/${videoId}`; %>
      <div class="movieDetail">
        <iframe
          width="560"
          height="315"
          src="<%= embedUrl %>"
          frameborder="0"
          allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
          allowfullscreen
        ></iframe>
      </div>
      <br /><br />
      <div class="inline">
        <a class="btn btn-outline-warning" href="/">Go Back</a>&emsp;
        <a class="btn btn-outline-warning" href="/movie/video?id=<%=id %>"
          >Watch Movie</a
        >
      </div>
    </body>
  </div>
  <div>
    <h2>Reviews</h2>

    <% if (movieObj.reviews && movieObj.reviews.length > 0) { %>
    <div class="reviews-container">
      <% movieObj.reviews.forEach((review) => { %>
      <div class="card review-card mb-3">
        <div class="card-body">
          <h5 class="card-title"><%= review.name %></h5>
          <p class="card-text"><%= review.review %></p>
          <p class="card-footer text-muted">
            <small><%= review.createdAt.toDateString() %></small>
          </p>
        </div>
      </div>
      <% }) %>
    </div>
    <% } else { %>
    <p>No reviews yet. Be the first to add a review!</p>
    <% } %>
  </div>
</html>
